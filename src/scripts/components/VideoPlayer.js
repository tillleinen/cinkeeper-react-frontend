'use strict';

var React = require('react/addons');

require('styles/VideoPlayer.sass');

var VideoPlayer = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for VideoPlayer</p>
        </div>
      );
  }
});

module.exports = VideoPlayer; 

