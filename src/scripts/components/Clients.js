'use strict';

var React = require('react/addons');

require('styles/Clients.sass');

var Clients = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for Clients</p>
        </div>
      );
  }
});

module.exports = Clients; 

